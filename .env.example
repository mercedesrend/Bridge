# Required: OpenAI API key used by the /api/match route.
OPENAI_API_KEY=your_key_here

# Optional: override the matching model (defaults to gpt-4.1-nano).
OPENAI_MODEL=gpt-4.1-nano

# Live visit companion (FastAPI phone UI). Defaults to local demo.
NEXT_PUBLIC_VISIT_APP_URL=http://localhost:8000
